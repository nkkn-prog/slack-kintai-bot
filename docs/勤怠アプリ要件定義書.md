# Slack 勤怠管理アプリ 要件定義書

## 1. プロジェクト概要

### 1.1 目的
Slackのメッセージをトリガーとして勤怠（稼働開始・稼働終了）を記録し、Google Spreadsheetに自動で打刻するアプリケーションを開発する。

### 1.2 背景
- 日常的に利用するSlack上で勤怠管理を完結させることで、打刻の手間を最小化する
- 同一日に複数の稼働時間帯が発生するケースに対応する必要がある

---

## 2. 機能要件

### 2.1 トリガーメッセージ

| メッセージ | 動作 |
|---|---|
| `稼働開始` | 稼働開始時刻をGoogle Spreadsheetに記録する |
| `稼働終了` | 稼働終了時刻をGoogle Spreadsheetに記録する |

- チャンネルへの直接投稿、およびスレッド内の投稿の両方で発火する
- あらゆるチャンネル（パブリック・プライベート）で利用可能とする
- メッセージは完全一致とする（例：「稼働開始します」では発火しない）

### 2.2 打刻記録

- 「稼働開始」→「稼働終了」で **1セット** とする
- 同一日に複数セットの記録が可能（例：午前稼働 → 中断 → 午後稼働）
- 日をまたぐ稼働も許容する（開始が3/29、終了が3/30のケースなど）

#### 記録データ項目（1行＝1セット）

| 列 | 項目 | 例 |
|---|---|---|
| A | 日付（稼働開始日） | 2026-03-29 |
| B | 稼働開始時刻 | 09:00:00 |
| C | 稼働終了時刻 | 18:00:00 |
| D | 稼働時間（自動計算） | 9:00:00 |
| E | 備考 | （将来拡張用） |

- 「稼働開始」打刻時：A列（日付）とB列（開始時刻）を記録。C列・D列は空欄。
- 「稼働終了」打刻時：直近の未終了レコード（C列が空欄の行）を検索し、C列に終了時刻を記入。D列に稼働時間を自動計算して記入。

### 2.3 Slack応答メッセージ

打刻時にSlack上でフィードバックメッセージを返す。

| トリガー | 応答メッセージ例 |
|---|---|
| 稼働開始（正常） | `:clock9: 稼働開始を記録しました（09:00）` |
| 稼働終了（正常） | `:clock5: 稼働終了を記録しました（18:00）稼働時間：9時間00分` |
| 稼働開始（未終了あり） | `:warning: 前回の稼働が未終了です。先に「稼働終了」を入力してください。` |
| 稼働終了（未開始） | `:warning: 稼働開始の記録がありません。先に「稼働開始」を入力してください。` |

### 2.4 異常系・アラート

- **12時間ルール**: 稼働開始から12時間経過しても稼働終了がない場合、該当ユーザーにDMまたは元チャンネルで通知を送る
  - 通知メッセージ例：`:rotating_light: 稼働開始から12時間が経過しました。稼働終了の入力を忘れていませんか？`
  - 通知は1回のみとする（繰り返し通知はしない）
- **二重開始の防止**: 未終了の稼働がある状態で「稼働開始」が打たれた場合、エラーメッセージを返し記録しない
- **未開始での終了防止**: 稼働開始がない状態で「稼働終了」が打たれた場合、エラーメッセージを返し記録しない

---

## 3. Google Spreadsheet 設計

### 3.1 スプレッドシート構成

- **ユーザーごとに1つのスプレッドシート** を作成する
  - ファイル名規則：`勤怠記録_<SlackユーザーID or 表示名>`
- **月ごとにシートを分ける**
  - シート名規則：`YYYY-MM`（例：`2026-03`）
  - ユーザーの初回打刻時、または月が変わった際に自動で新しいシートを作成する

### 3.2 シートレイアウト

```
| A列: 日付 | B列: 稼働開始 | C列: 稼働終了 | D列: 稼働時間 | E列: 備考 |
|-----------|-------------|-------------|-------------|----------|
| 2026-03-29| 09:00:00    | 12:30:00    | 3:30:00     |          |
| 2026-03-29| 14:00:00    | 18:00:00    | 4:00:00     |          |
| 2026-03-30| 10:00:00    |             |             |          |
```

- 1行目はヘッダー行とする
- シート最下部に月の合計稼働時間を自動計算する行を設ける（将来検討）

---

## 4. 非機能要件

### 4.1 対応範囲
- Slackワークスペース内のすべてのパブリックチャンネル・プライベートチャンネルで利用可能
- アプリがチャンネルに参加している（招待されている）ことが前提

### 4.2 タイムゾーン
- 打刻時刻はJST（Asia/Tokyo）を基準とする

### 4.3 スケーラビリティ
- 将来的に複数ユーザーが利用する想定
- 新規ユーザーが初めて打刻した際に、スプレッドシートを自動作成する（管理者の事前作業は不要）
- ユーザー追加時はスプレッドシートの自動作成で対応（アプリ側のコード変更は不要）

### 4.4 セキュリティ
- Google Spreadsheetへのアクセスはサービスアカウント経由とし、各ユーザーのスプレッドシートに対して適切な共有権限を設定する
- SlackのBot Token Scopesは最小権限の原則に従う

### 4.5 可用性
- 12時間アラートのためのスケジュール実行が必要（定期実行 or イベント駆動）

---

## 5. 技術スタック

### Google Apps Script（GAS）ベース

| 要素 | 技術 |
|---|---|
| Slackイベント受信 | Slack Events API → GAS Web App（doPost） |
| ロジック実行 | Google Apps Script |
| データ保存 | Google Spreadsheet（SpreadsheetApp） |
| 12時間アラート | GASの時間駆動型トリガー（1時間ごと等で未終了チェック） |
| デプロイ | GAS Web Appとして公開 |

**留意事項**:
- GASの1回の実行時間上限は6分。通常の打刻処理では問題にならない。
- 12時間アラートは時間駆動型トリガーで定期的に未終了レコードをスキャンする方式とする。
- ユーザーとスプレッドシートの対応関係は、管理用スプレッドシート（マスタ）で管理する。

---

## 6. Slack App 設定要件

### 6.1 必要なBot Token Scopes

| スコープ | 用途 |
|---|---|
| `channels:history` | パブリックチャンネルのメッセージ読み取り |
| `groups:history` | プライベートチャンネルのメッセージ読み取り |
| `chat:write` | 応答メッセージの送信 |
| `im:write` | DM通知の送信（12時間アラート用） |
| `users:read` | ユーザー情報の取得（表示名の取得等） |

### 6.2 Events API サブスクリプション

| イベント | 用途 |
|---|---|
| `message.channels` | パブリックチャンネルのメッセージ監視 |
| `message.groups` | プライベートチャンネルのメッセージ監視 |

---

## 7. ユーザーフロー

```
[ユーザー]  →  Slackで「稼働開始」を投稿
                    ↓
[Slack API] →  Events APIでアプリにイベント送信
                    ↓
[アプリ]    →  ユーザーID特定 → 対象スプレッドシート特定
            →  該当月シートに開始時刻を記録
            →  Slackに応答メッセージを返信
                    ↓
            （稼働中...）
                    ↓
[ユーザー]  →  Slackで「稼働終了」を投稿
                    ↓
[アプリ]    →  未終了レコードを検索 → 終了時刻を記入
            →  稼働時間を計算・記入
            →  Slackに応答メッセージを返信
```

---

## 8. 将来拡張案（スコープ外）

- 勤怠修正コマンド（例：`稼働修正 09:00 18:00`）
- 日次・週次・月次の稼働レポート自動投稿
- 休憩時間の記録対応（`休憩開始` / `休憩終了`）
- 管理者向けダッシュボード（全ユーザーの稼働状況一覧）
- 勤怠チャンネル専用モード（特定チャンネルのみで反応）
- スプレッドシートへのリンクをSlack応答に含める

---

## 9. 確定済みの設計判断

| 項目 | 決定内容 |
|---|---|
| ユーザー登録フロー | 初回打刻時にスプレッドシートを自動作成 |
| メッセージ判定方式 | 完全一致（「稼働開始」「稼働終了」のみ反応） |
| 技術スタック | Google Apps Script（GAS） |

## 10. 未決事項（要検討）

1. **スプレッドシートの保存場所**: Google Driveのどのフォルダに格納するか
